Jogos

Sera necessário chamar esse endpoint com o id tanto do campeonato brasileiro quanto da copa do brasil e filtrar pela data selecionada

GET https://api.api-futebol.com.br/v1/campeonatos/:campeonato_id/partidas

Dados necessários:

PartidaId
CampeonatoId
NomeCampeonato
LogoTime
Placar
Data
Horario

Não tem o nome do campeonato na response desse endpoint. 

Não tem informações do placar da partida

Deverá confirmar qual a estrutura do response da api para o campeonato brasileiro, pois só esta mostrando a estrutura da copa do brasil

Estrutura retornada na copa do brasil:

{
	partidas: {
		partidas: {
			fase: {
				chave: {
					partida_id: int
				}
			}
		}
	}
}


Será necessário obter a partida pelo id para preencher mais detalhes como placar

GET https://api.api-futebol.com.br/v1/partidas/:partida_id

Estrutura retornada pela api contendo apenas os dados necessários para a tela:

{
	partida_id: int,
	campeonato: {
		campeonato_id: int,
		nome: string,
		nome_popular: string
	},
	time_visitante: {
		time_id: int,
		nome_popular: string,
		escudo: string
	},
	placar_mandante: int,
	placar_visitante: int,
	status: string,
	data_realizacao: string,
	hora_realizacao: string
}
